# LeafLink Configuration
# Copy this file to .env and fill in your actual values

# Required: LeafLink API key for authentication
# Get this from your LeafLink account (App or User token)
# Format: Authorization: App {api_key}
TAP_LEAFLINK_API_KEY=your_api_key_here

# Optional: LeafLink API base URL
# Production (default): https://app.leaflink.com/api/v2
# Sandbox: https://www.sandbox.leaflink.com/api/v2
# Integrations: https://www.leaflink-integrations.leaflink.com/api/v2
TAP_LEAFLINK_API_URL=https://app.leaflink.com/api/v2

# Optional: The earliest record date to sync (ISO 8601 format)
# Used for incremental replication on streams that support it
TAP_LEAFLINK_START_DATE=2024-01-01T00:00:00Z
